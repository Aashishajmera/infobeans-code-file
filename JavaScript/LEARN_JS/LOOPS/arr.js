const arr = [1, 2, 3, 4, 5];

const newArr = arr.map((Element) => {
return  Element *= 2;
});

const newTwo = arr.forEach(element =>{
    return element *8;
})

console.log(arr);
console.log(newArr);
